<? if (data._include.director_or_member == "Member") { ?>
<para_1><?!= plural(data.parties.shareholder, "I", "We") ?>, the undersigned, being the <?!= plural(data.parties.shareholder, "sole member", "Shareholders") ?> of the Company, do hereby pass the foregoing Resolution<?= data.members_resolution_2 ? "s" : "" ?> by written means:---</para_1>
<? } ?>

<? if (! (data._config.omit_date && data._config.omit_date.value)) { ?>
<para_1><span_smallcaps>Dated this:</span_smallcaps> <?= data.resolution_date || data._include.date || data._todays_date ?></para_1>
<? } ?>

<para_1>This is a development branch, refactoring to use inc_enumerate_signatures instead of hardcoding the parties as "shareholder" and "director".</para_1>


<? if (data._include.director_or_member == "Member") { ?>
<para_1>previously we would have used data.parties.shareholder, which gives <?= data.parties.shareholder.map(function(p) { return p.name }).join(", ") ?></para_1>
<? } else { ?>
<para_1>previously we would have used data.parties.director, which gives <?= data.parties.director.map(function(p) { return p.name }).join(", ") ?></para_1>
<? } ?>

<para_1>instead, we're going to use rcpts_to. let's see how that looks.</para_1>

<?!= include("inc_enumerate_signatures", data, {rcpts_to:data._include.rcpts_to,
  _include2: { term:{brand_new_investor: "new investor", shareholder:"current shareholder"} } }) ?>

<? Logger.log("_include = %s", data._include); ?>
<? Logger.log("butWaitTheresMore = %s", data._include.butWaitTheresMore); ?>
<? if (! (data._include.butWaitTheresMore)) { ?>
</Story_Main>
<Bottom_Box delete_if_empty="true" />
</Root>
<? } ?>
